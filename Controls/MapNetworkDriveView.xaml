<UserControl
    x:Class="FastExplorer.Controls.MapNetworkDriveView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="12" Width="360">
        <StackPanel Spacing="4">
            <TextBlock Text="Letra da unidade" Opacity="0.6" FontSize="12" />
            <ComboBox x:Name="DriveLetterCombo" HorizontalAlignment="Stretch" />
        </StackPanel>

        <StackPanel Spacing="4">
            <TextBlock Text="Pasta" Opacity="0.6" FontSize="12" />
            <TextBox x:Name="PathBox" PlaceholderText="\\servidor\pasta" />
        </StackPanel>

        <StackPanel Spacing="4">
            <TextBlock Text="Usuário (opcional)" Opacity="0.6" FontSize="12" />
            <TextBox x:Name="UsernameBox" PlaceholderText="Deixe em branco para usar o usuário atual" />
        </StackPanel>

        <StackPanel Spacing="4">
            <TextBlock Text="Senha (opcional)" Opacity="0.6" FontSize="12" />
            <PasswordBox x:Name="PasswordBox" />
        </StackPanel>

        <CheckBox x:Name="ReconnectCheck" Content="Reconectar ao entrar" IsChecked="True" />

        <TextBlock x:Name="ErrorText" Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                   TextWrapping="Wrap" Visibility="Collapsed" />
    </StackPanel>
</UserControl>
